<?xml version="1.0" encoding="utf-8"?>
<!--
  Preferences (including the chosen server) are backed up; nothing else exists yet.
  When device tokens land they must be excluded here — a credential does not belong
  in a cloud backup.
-->
<full-backup-content>
    <include domain="sharedpref" path="." />
</full-backup-content>
